#include <stdio.h>

#define DIM 21

int main(void){
	
	char parola[DIM];
	char anagramma [DIM];

	scanf("%20s",parola);
	
	for(int i=0; i<DIM && paola[i] !='\0'; i++)
	{
		pos=rand % strlen(parola);
		
		anagramma[pos]=parola[i];
		}	
	
	
	return (0);
}
